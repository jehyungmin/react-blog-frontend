import React from 'react';
import styles from './Footer.scss';
import className from  'classNames.bind';

const cx = classNames.bind(styles);

const Footer = () => (
    <div className={cx('footer-template')}>
        Footer
    </div> 
)